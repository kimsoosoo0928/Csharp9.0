using System;

namespace ch03_03
{
    class Program
    {
        static void Main(string[] args)
        {
            //int sum = 0;
            //int n = 1;

            //while (n <= 1000)
            //{
            //    if(n%2 == 0)
            //    {
            //        sum += n;
            //    }
            //    n++;
            //}
            //Console.WriteLine(sum); // 출력 결과 : 250500

            //int sum = 0;
            //for (int n = 1; n <=1000; n++)
            //{
            //    if (n%2==0)
            //    {
            //        sum += n;
            //    }
            //}

            //int sum = 0;
            //int n = 0; // 초기값이 0으로 변경됨
            //do
            //{
            //    if (n % 2 == 0) sum += n;
            //} while (++n <= 1000);

            //int i = 2;
            //while (true)
            //{
            //    int j = 1;
            //    while (true)
            //        Console.WriteLine(i + "*" + j + "=" + (i * j));

            //    if (j++ > 9)
            //    {
            //        break;
            //    }
            //}

            //if(i++ > 9)
            //{
            //    break;
            //}

            //int sum = 0;
            //int n = 1;
            //while(n++ <=1000)
            //{
            //    if((n%2) != 0)
            //    {
            //        continue; // ssumg += n; 구문을 건너뛰거, while 문의 조건식평가로 실행을 해준다.
            //    }
            //    sum += b;

            // sum += 구문을 건너뛰고, 들어쓰기 블록도 줄이는 역할도한다.


            //int sum = 0;
            //int n = 1;
            //while (n++ <= 1000)

            ////if ((n % 2) == 0)
            ////{
            ////    if ((n % 3) == 0)
            ////    {
            ////        if ((n % 5) == 0)
            ////        {
            ////            sum += n;
            ////        }
            ////    }
            ////}
            //{
            //    if ((n%2) != 0) continue;
            //    if ((n % 3) != 0) continue;
            //    if ((n % 5) != 0) continue;

            //    sum += n;

            //int sum = 0;
            //int n = 0;


//            bool exitLoop = false;

//            for (int x = 2; x < 10; x++)
//            {
//                for (int y = 1; y < 10; y++)
//                { Console.WriteLine(x + "*" + y + "=" + (x * y));

//                    exitLoop = x == 5 && y == 8;
//                    if (exitLoop == true)
//                    {
//                        break;
//                    }
//                }
//                if (exitLoop == true)
//                { break; }
//            }
//        }
//    }
//}

        //    for (int x = 2; x <10; x++)
        //    {
        //        for (int y=1; y <10; y++)
        //        {
        //            Console.WriteLine(x + "*" + y + " = " +(x * y));

        //            if (x == 5 && y == 8) goto LOOP_EXIT;
        //        }
        //    }

        //LOOP_EXIT:;













































































































































































































        }
    }
}

